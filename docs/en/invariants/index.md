# Invariants

Kernel-level assumptions that must not drift.

## Purpose

- preserve structural stability
- define the minimal shared substrate required by downstream repositories
- constrain identifiers and shared constants without embedding interpretation
